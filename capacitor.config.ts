import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matirivoira.JuegoMemoria',
  appName: 'JuegoMemoria',
  webDir: 'www'
};

export default config;
